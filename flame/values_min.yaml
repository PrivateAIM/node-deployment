# Global values
global:
  hub:
    auth:
      robotUser: ""
      robotSecret: ""
  node:
    ingress:
      enabled: true
      hostname: "https://my.domain.de"
  keycloak:
    ## @param global.keycloak.hostname Hostname for a separate instance of keycloak. Overrides the /keycloak subpath
    hostname: ""

# keycloak values
keycloak:
  auth:
    adminUser: admin
    adminPassword: admin

  ingress:
    enabled: true

  ## @param keycloak.proxyHeaders Set Keycloak proxy headers to "forwarded" or "xforwarded"
  ## This needs to be set if SSL/TLS is being used, else leave blank
  proxyHeaders: "forwarded"
